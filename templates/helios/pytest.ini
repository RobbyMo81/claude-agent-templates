[pytest]
# Prevent pytest from recursing into archive folders which contain duplicate test modules
norecursedirs = archive archive_disabled archive_disabled/* archive_backup
# Keep default test discovery, but you can tune python_files/python_classes if needed
